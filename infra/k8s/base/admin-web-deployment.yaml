apiVersion: apps/v1
kind: Deployment
metadata:
  name: admin-web
  namespace: cme-trading
spec:
  replicas: 2
  selector:
    matchLabels:
      app: admin-web
  template:
    metadata:
      labels:
        app: admin-web
    spec:
      containers:
      - name: admin-web
        image: cme-admin-web:latest
        ports:
        - containerPort: 3002
        env:
        - name: NODE_ENV
          valueFrom:
            configMapKeyRef:
              name: cme-config
              key: NODE_ENV
        - name: NEXT_PUBLIC_API_URL
          value: "http://api-service:3001"
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /
            port: 3002
          initialDelaySeconds: 40
          periodSeconds: 10
          timeoutSeconds: 5
        readinessProbe:
          httpGet:
            path: /
            port: 3002
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
---
apiVersion: v1
kind: Service
metadata:
  name: admin-web-service
  namespace: cme-trading
spec:
  selector:
    app: admin-web
  ports:
    - protocol: TCP
      port: 3002
      targetPort: 3002
  type: ClusterIP
