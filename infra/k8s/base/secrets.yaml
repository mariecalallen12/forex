apiVersion: v1
kind: Secret
metadata:
  name: cme-secrets
  namespace: cme-trading
type: Opaque
stringData:
  # ⚠️ WARNING: This is a template for base configuration
  # DO NOT use in production! Override with actual secrets in overlays/prod/secrets.env
  # For development only with default values
  
  # Database credentials
  POSTGRES_USER: "postgres"
  POSTGRES_PASSWORD: "dev_password_123"
  DATABASE_USER: "postgres"
  DATABASE_PASSWORD: "dev_password_123"
  
  # JWT secrets (must be overridden in production)
  JWT_SECRET: "dev_jwt_secret_key_do_not_use_in_prod"
  JWT_REFRESH_SECRET: "dev_jwt_refresh_secret_do_not_use_in_prod"
  
  # CORS origin
  CORS_ORIGIN: "http://localhost:3000,http://localhost:3002"
