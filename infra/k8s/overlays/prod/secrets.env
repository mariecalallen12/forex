# ⚠️ CRITICAL: Production secrets - MUST be replaced before deployment!
# These are placeholder values. The deployment script will validate they've been changed.
# Generate strong secrets using: openssl rand -base64 32

POSTGRES_USER=postgres
POSTGRES_PASSWORD=REPLACE_WITH_STRONG_PASSWORD
DATABASE_USER=postgres
DATABASE_PASSWORD=REPLACE_WITH_STRONG_PASSWORD
JWT_SECRET=REPLACE_WITH_STRONG_JWT_SECRET_AT_LEAST_32_CHARS
JWT_REFRESH_SECRET=REPLACE_WITH_STRONG_REFRESH_SECRET_AT_LEAST_32_CHARS
CORS_ORIGIN=https://app.cme-trading.com,https://admin.cme-trading.com

# To generate strong secrets:
# openssl rand -base64 32
